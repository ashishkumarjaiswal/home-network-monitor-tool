[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
# Keep lockfile in sync; Coolify Nixpacks defaults to npm ci
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "npm run start"
